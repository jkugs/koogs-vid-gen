# AI Video Project — Session 1 Handoff

## Where We Left Off

Iteration 1 now has a minimal repository, a working RunPod environment, the Wan
2.2 model cached on persistent storage, and a first implementation of the
image-to-video script. At the end of this session, the first real video
generation was reported as running. Its result has not yet been reviewed or
recorded.

Read `dev-docs/001.md` for the full project scope and `AGENTS.md` for working
conventions.

## Decisions

- Use `Wan-AI/Wan2.2-TI2V-5B-Diffusers` for the first experiment.
- Use the Diffusers `WanImageToVideoPipeline`, not ComfyUI or a cloned Wan
  repository.
- Use Python 3.11 with `uv`.
- Pin PyTorch 2.8.0 and Torchvision 0.23.0 to the CUDA 12.8 wheel index so they
  match the selected RunPod template.
- Generate approximately five seconds at 24 FPS using 121 frames and 50
  inference steps.
- Keep the implementation explicit and single-model. Do not build application
  infrastructure or generalized abstractions during Iteration 1.

## Repository State

Important commits created during setup:

```text
da648e5  Set up video generation project dependencies
7a75230  Updating torch uv pins for RunPod
860e54c  Initializing some structure to the repo
f0dc638  Initial generate_video implementation
```

The repository contains:

```text
generate_video.py
inputs/
outputs/
dev-docs/001.md
dev-docs/002.md
AGENTS.md
pyproject.toml
uv.lock
```

`inputs/`, `outputs/`, caches, model weights, and common video files are ignored
by Git. Personal photographs and generated videos must not be committed.

At the time this handoff was written, `AGENTS.md` and `dev-docs/002.md` had not
yet been committed.

## Current Script

`generate_video.py` currently:

- accepts `--image`, `--prompt`, `--output`, and `--seed`;
- defaults to `inputs/input.jpg` and `outputs/output.mp4`;
- validates the image and prompt before importing heavy libraries;
- requires CUDA;
- loads the Wan VAE in float32 and the pipeline in bfloat16;
- preserves the input aspect ratio within a `1280 * 704` target area;
- generates 121 frames at 24 FPS with 50 steps and guidance scale 5.0;
- exports a standard MP4;
- reports the GPU, parameters, resolution, loading time, inference time, export
  time, total time, and VRAM use;
- prints stage markers so long operations do not appear frozen.

Local checks passed for syntax, CLI defaults, missing/invalid inputs, dimension
calculation, lockfile integrity, and a mocked end-to-end workflow. A full local
Diffusers import was blocked because the workstation's asdf Python 3.11 build
lacks the standard `_bz2` extension. This is a workstation Python issue, not a
project dependency issue.

## RunPod State

The working environment uses:

```text
GPU:       NVIDIA L40S, 48 GB VRAM
Template:  runpod/pytorch:1.0.2-cu1281-torch280-ubuntu2404
PyTorch:   2.8.0+cu128
CUDA:      12.8
Storage:   100 GB independent network volume mounted at /workspace
```

The original GPU became unavailable after the pod was stopped, so the pod was
migrated/recreated. The network volume worked as intended and retained the
repository, virtual environment, caches, and model.

Persistent paths:

```text
/workspace/koogs-vid-gen
/workspace/koogs-vid-gen/.venv
/workspace/.cache/uv
/workspace/.cache/huggingface
/workspace/pod-env.sh
```

`pod-env.sh` must be sourced, not executed:

```bash
source /workspace/pod-env.sh
```

It defines:

```text
HF_HOME=/workspace/.cache/huggingface
UV_CACHE_DIR=/workspace/.cache/uv
```

The complete 34.2 GB model is cached at revision
`b8fff7315c768468a5333511427288870b2e9635` under:

```text
/workspace/.cache/huggingface/hub/models--Wan-AI--Wan2.2-TI2V-5B-Diffusers
```

RunPod smoke tests passed:

```text
bz2: OK
Diffusers imports: OK
Torch: 2.8.0+cu128
GPU: NVIDIA L40S
```

## First Generation

A private photograph named `me.jpg` was placed in the ignored `inputs/`
directory on the pod. The intended first run was:

```bash
cd /workspace/koogs-vid-gen
source /workspace/pod-env.sh
uv run --no-sync python generate_video.py \
  --image inputs/me.jpg \
  --output outputs/me-smile.mp4
```

The default prompt asks the person to slowly turn toward the camera and smile
while the camera remains stationary.

## Next Steps

1. Confirm whether the first generation completed or failed.
2. Capture the console output, especially model-loading time, inference time,
   peak VRAM, resolution, and total time.
3. Download `outputs/me-smile.mp4` and evaluate motion, identity retention, and
   obvious artifacts.
4. Fix only issues revealed by the real run, then repeat with the same image and
   seed when useful.
5. Record the first experiment and operating instructions in the README or a
   later development note.
6. Commit `AGENTS.md`, this handoff, and any verified fixes when requested.

Remember that GPU billing continues whenever a pod is running, even when the
GPU is idle. Terminate the compute pod when it is not needed; the independent
network volume will preserve `/workspace`.
